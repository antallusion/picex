version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: picex
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: always
